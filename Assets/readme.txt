zfc.exe -i "..\..\..\Assembly-CSharp.csproj" -o "ZeroFormatterGenerated.cs"

После удаления старого матча данные клиенту из нового матча отправляются, но данные ввода не принимаются.
Варианты:
1) сервер не принимает вход из неизвестного ip
2) клиент не отправляет вход


Удаление матча ломает логику других матчей, в которых учавствует игрок.
! Посмотреть на код удаления матча.